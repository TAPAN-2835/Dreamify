// Legacy worker file kept for reference. Background workers are disabled in this deployment mode.

const worker = null;

console.log('[Worker] Legacy worker file loaded, but background workers are disabled in this deployment mode.');

export default worker;
